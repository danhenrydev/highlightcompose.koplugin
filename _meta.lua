local _ = require("gettext")
return {
    name = "highlightcompose",
    fullname = _("Integrated highlight & note bar"),
    description = _([[Replaces the text-selection popup with a single panel: pick highlight color and style, adjust selection edges, add a note, open extra actions (translate, Wikipedia, …), then confirm with the checkmark.]]),
}
